import styled from "styled-components";
import { Layout } from "../../Layout";

export const Container = styled(Layout)`
  height: 100vh;

  h1 {
    margin-top: auto;
  }

  h2 {
    margin-bottom: auto;
  }
`;
