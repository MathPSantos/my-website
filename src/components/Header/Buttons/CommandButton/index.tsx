import { IconButton } from "../../../IconButton";

import { Command } from "../../../../styles/icons";

export function CommandButton() {
  return (
    <IconButton isHide>
      <Command />
    </IconButton>
  );
}
